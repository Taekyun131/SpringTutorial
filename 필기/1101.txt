--1031 문제풀이

1. html 요소구성
2. 동적 데이터처리

1번문제
1) 키를 클릭하면 text 수식으로 만들어진다
  1-1) 키를 클릭하면 JS호출
  1-2) 호출된 함수는 키의 값을 가져온다
  1-3) 가져온 키로 text 수식을 만든다
2) 키 중에 "="을 클릭하면 수식을 계산한다

2번문제 
js 알고리즘 순서
1) 아이디에 입력된 값 가져온다>> 기술: getElementById
2) 비번 입력된 값 가져온다
3) 아이디가 8글자 이상인지?
4) 비밀번호가 4글자 이상인지?

콜백함수: 다른 함수의 파라미터로써 전달되는 함수
	콜백은 태스크가 끝나기 전에 함수가 실행되지 않는 것을 보장합니다. 
	다르게 말하자면 콜백은 그 태스크가 끝난 직후에 실행될 것입니다. 
	콜백은 비동기 자바스크립트 코드를 작성할 수 있도록 해주고 
	여러 문제와 에러들로부터 안전하게 지켜줍니다.

**.value와 .innerText와 textContent 차이점
 -value: input과 같은 form 요소의 값을 가져오고 싶을 때는 .value 를 사용한다.
 -innerText: innerText는 'Element'의 속성
	  해당 Element 내에서 사용자에게 '보여지는' 텍스트 값을 읽어온다.
	  내용에 연속되는 공백이 있다면 연속공백을 무시고 하나의 공백만 처리하여 보여진다.
	  display : none; 한 값들은 불러들이지 않는다.
 -textContent: textContent는 'Node'의 속성
	      innetText와는 달리 <script>나 <style> 태그와 상관없이 해당 노드가 가지고 있는 텍스트 값을 그대로 읽는다.
	      연속된 공백이 그대로 보여진다.
	      'display:none' 스타일이 적용된 '숨겨진 텍스트' 문자열도 그대로 출력된다.